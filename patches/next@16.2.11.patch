diff --git a/dist/build/index.js b/dist/build/index.js
index 5f5935c1e86f531d35dfd6b7ba7c57e21610cca9..b13ce2e7e1770335892a3e2b98e2b845be4d363c 100644
--- a/dist/build/index.js
+++ b/dist/build/index.js
@@ -1371,7 +1371,19 @@ async function build(dir, experimentalAnalyze = false, reactProductionProfiling
                                                 // - It has no dynamic param
                                                 // - It doesn't have generateStaticParams but `dynamic` is set to
                                                 //   `error` or `force-static`
-                                                if (!isDynamic) {
+                                                // Patched: force /_global-error/page to skip static marking.
+                                                // Its forced static-export path crashes with
+                                                // `TypeError: null is not an object (evaluating 'k.H.useContext')`
+                                                // during "Generating static pages" — reproduced identically on
+                                                // next@16.2.10, 16.2.11, and 16.3.0-preview.7, with a bare-minimum
+                                                // layout/global-error.tsx and with neither at all. Genuine upstream
+                                                // bug unrelated to app code (see bun patch / patches/next.patch).
+                                                // Rendering it dynamically instead routes around the broken path;
+                                                // global-error is only ever hit if the root layout itself throws,
+                                                // so this has no real-world runtime cost.
+                                                if (originalAppPath === "/_global-error/page") {
+                                                // fall through: leave isStatic unset (dynamic)
+                                                } else if (!isDynamic) {
                                                     staticPaths.set(originalAppPath, [
                                                         {
                                                             params: {},
diff --git a/dist/build/utils.js b/dist/build/utils.js
index 82b6eb4cf861a1cbe0bee524cbdf374a5d8d6cf9..668258d0c412423436ab1292c1b19ee693eed26e 100644
--- a/dist/build/utils.js
+++ b/dist/build/utils.js
@@ -569,7 +569,17 @@ async function isPageStatic({ dir, page, distDir, configFileName, httpAgentOptio
     // Skip page data collection for synthetic _global-error routes
     if (page === _constants1.UNDERSCORE_GLOBAL_ERROR_ROUTE) {
         return {
-            isStatic: true,
+            // Patched (see bun patch / patches/next.patch): forcing this
+            // isStatic crashes during "Generating static pages" with
+            // `TypeError: null is not an object (evaluating 'k.H.useContext')`
+            // — reproduced identically on next@16.2.10, 16.2.11, and
+            // 16.3.0-preview.7, with a bare-minimum layout/global-error.tsx
+            // and with none at all. Genuine upstream bug in the forced
+            // static-export path for this synthetic route, unrelated to
+            // app code. Rendering it dynamically instead routes around the
+            // broken path; global-error is only ever hit if the root layout
+            // itself throws, so this has no real-world cost.
+            isStatic: false,
             isRoutePPREnabled: false,
             prerenderFallbackMode: undefined,
             prerenderedRoutes: undefined,
